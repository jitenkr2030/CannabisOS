'use client'

import { useState, useEffect } from 'react'
import { Button } from '@/components/ui/button'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Users, Target, Lightbulb, Award, MapPin, Mail, Phone } from 'lucide-react'
import Footer from '@/components/ui/Footer'

export default function AboutPage() {
  const [isClient, setIsClient] = useState(false)

  useEffect(() => {
    setIsClient(true)
  }, [])
  const team = [
    {
      name: "John Smith",
      role: "CEO & Founder",
      description: "15+ years in cannabis industry compliance and technology"
    },
    {
      name: "Sarah Johnson",
      role: "CTO",
      description: "Former Google engineer with expertise in scalable systems"
    },
    {
      name: "Mike Chen",
      role: "Head of Product",
      description: "Product leader with experience in enterprise SaaS"
    }
  ]

  const values = [
    {
      icon: <Target className="h-8 w-8" />,
      title: "Customer Success",
      description: "We're committed to helping our customers succeed and grow their businesses."
    },
    {
      icon: <Lightbulb className="h-8 w-8" />,
      title: "Innovation",
      description: "Continuously improving our platform with cutting-edge technology and features."
    },
    {
      icon: <Users className="h-8 w-8" />,
      title: "Integrity",
      description: "Operating with transparency, honesty, and ethical business practices."
    }
  ]

  return (
    <div className="min-h-screen bg-gray-50">
      {/* Hero Section */}
      <section className="bg-gradient-to-br from-purple-600 to-indigo-700 text-white py-20">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="text-center">
            <h1 className="text-4xl md:text-6xl font-bold mb-6">
              About CannabisOS
            </h1>
            <p className="text-xl md:text-2xl text-purple-100 max-w-3xl mx-auto mb-8">
              Empowering dispensaries with technology that drives success and ensures compliance
            </p>
          </div>
        </div>
      </section>

      {/* Mission */}
      <section className="py-20">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="text-center mb-16">
            <h2 className="text-3xl md:text-4xl font-bold text-gray-900 mb-4">
              Our Mission
            </h2>
            <p className="text-xl text-gray-600 max-w-3xl mx-auto">
              To provide cannabis businesses with the tools they need to operate efficiently, 
              stay compliant, and thrive in a competitive market.
            </p>
          </div>

          <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
            {values.map((value, index) => (
              <Card key={index} className="text-center border-0 shadow-sm">
                <CardContent className="p-8">
                  <div className="w-16 h-16 bg-purple-100 rounded-full flex items-center justify-center text-purple-600 mx-auto mb-4">
                    {value.icon}
                  </div>
                  <h3 className="text-xl font-semibold mb-2">{value.title}</h3>
                  <p className="text-gray-600">{value.description}</p>
                </CardContent>
              </Card>
            ))}
          </div>
        </div>
      </section>

      {/* Team */}
      <section className="py-20 bg-white">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="text-center mb-16">
            <h2 className="text-3xl md:text-4xl font-bold text-gray-900 mb-4">
              Meet Our Team
            </h2>
            <p className="text-xl text-gray-600 max-w-3xl mx-auto">
              The experts behind CannabisOS
            </p>
          </div>

          <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
            {team.map((member, index) => (
              <Card key={index} className="text-center border-0 shadow-sm">
                <CardContent className="p-8">
                  <div className="w-20 h-20 bg-gray-200 rounded-full flex items-center justify-center mx-auto mb-4">
                    <span className="text-2xl font-bold text-gray-600">
                      {member.name.split(' ').map(n => n[0]).join('')}
                    </span>
                  </div>
                  <h3 className="text-xl font-semibold mb-1">{member.name}</h3>
                  <p className="text-purple-600 font-medium mb-2">{member.role}</p>
                  <p className="text-gray-600 text-sm">{member.description}</p>
                </CardContent>
              </Card>
            ))}
          </div>
        </div>
      </section>

      {/* Contact */}
      <section className="py-20">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="bg-gray-900 text-white rounded-lg p-8">
            <div className="text-center mb-8">
              <h2 className="text-3xl font-bold mb-4">Get in Touch</h2>
              <p className="text-gray-300">Have questions? We'd love to hear from you.</p>
            </div>
            <div className="grid grid-cols-1 md:grid-cols-3 gap-8 text-center">
              <div>
                <Mail className="h-8 w-8 mx-auto mb-2" />
                <p className="font-medium">Email</p>
                <p className="text-gray-300">info@cannabisos.com</p>
              </div>
              <div>
                <Phone className="h-8 w-8 mx-auto mb-2" />
                <p className="font-medium">Phone</p>
                <p className="text-gray-300">1-800-CANNABIS</p>
              </div>
              <div>
                <MapPin className="h-8 w-8 mx-auto mb-2" />
                <p className="font-medium">Office</p>
                <p className="text-gray-300">Toronto, ON, Canada</p>
              </div>
            </div>
          </div>
        </div>
      </section>

      <Footer />
    </div>
  )
}